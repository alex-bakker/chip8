{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${workspaceFolder}/driver.cc",
                "${workspaceFolder}/DFA.cc",
                "${workspaceFolder}/Token.cc",
                "${workspaceFolder}/ScanningExceptions.cc",
                "${workspaceFolder}/builder.cc",
                "-o",
                "assemble.out"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}